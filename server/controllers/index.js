const { User, ParkingSpace, NonMemberParking, MemberRegistration, MemberParking } = require('../models')
const { comparePassword } = require('../helpers/bcrypt')
const { sign } = require('../helpers/jwtHelper')

class Controller {

}

module.exports = Controller